services:
  - type: web
    name: phoenix
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerCommand: ""
    plan: starter
    healthCheckPath: /healthz
    envVars:
      - key: VITE_OBS_AGENT_URL
        value: ""  # Set to your obs_agent URL after deploying it first
      - key: PHOENIX_SQL_DATABASE_URL
        fromDatabase:
          name: phoenix-db
          property: connectionString
      - key: PHOENIX_SECRET
        generateValue: true
      - key: PHOENIX_ENABLE_AUTH
        value: "true"

databases:
  - name: phoenix-db
    plan: starter
